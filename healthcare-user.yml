spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3307/${DB_USER_NAME}
    username: ${DB_MYSQL_USER}
    password: ${DB_MYSQL_PASSWORD}
  cloud:
    consul:
      discovery:
        port: 8081
server:
  port: 8081
