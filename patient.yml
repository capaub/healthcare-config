spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3307/${DB_PATIENT_NAME}
    username: ${DB_MYSQL_USER}
    password: ${DB_MYSQL_PASSWORD}
  cloud:
    consul:
      discovery:
        port: 8082

server:
  port: 8082
